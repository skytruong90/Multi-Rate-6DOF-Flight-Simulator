[project]
name = "multi-rate-6dof-flight-simulator"
version = "0.1.0"
description = "Synthetic multi-rate aerospace flight simulation portfolio project"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
